services:

  backend:
    build:
      context: ./backend
    container_name: assessment-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: assessment-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend